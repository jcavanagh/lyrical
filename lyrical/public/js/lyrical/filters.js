if (typeof define !== 'function') { var define = require('amdefine')(module); }

/**
 * App filters
 * 
 * @author Joe Cavanagh
 */
define([
    
], function() {
    'use strict';
     
    return true;
});
